# Public API base URL for the frontend (used by the browser)
NEXT_PUBLIC_API_URL=

# Public Socket.IO server URL (used by the browser to connect to websockets)
NEXT_PUBLIC_SOCKET_URL=

# Secret key used to sign/verify JWT tokens on the backend
JWT_SECRET=
